{
  "name": "@rami/luma-enricher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "luma-enricher": "./dist/src/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run tests",
    "test:watch": "vitest",
    "lint": "biome check .",
    "format": "biome check --write .",
    "prepare-browser": "bunx playwright install chromium",
    "postinstall": "bunx playwright install chromium"
  },
  "dependencies": {
    "commander": "^14.0.2",
    "node-ical": "^0.22.1",
    "pino": "^10.1.0",
    "pino-pretty": "^13.1.2",
    "playwright": "1.60.0",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.7",
    "@types/node": "^24.10.1",
    "msw": "^2.12.3",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  },
  "engines": {
    "node": ">=20"
  }
}
